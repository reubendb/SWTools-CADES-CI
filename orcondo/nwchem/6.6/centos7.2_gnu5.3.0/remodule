  ### Set Environment (do not remove this line only change what is in between)
  . ${MODULESHOME}/init/ksh
  module unload PE-gnu
  module unload PE-intel
  module unload PE-pgi
  module load PE-gnu
  ### End Environment (do not remove this line only change what is in between)

PACKAGE=nwchem
VERSION=6.6
SRCDIR=${PACKAGE}-${VERSION}
DISTNAME=Nwchem-6.6.revision27746-src.2015-10-20

export NWCHEM_TOP=${SW_BLDDIR}/${SRCDIR}

export NWCHEM_MODULES=all
export NWCHEM_TARGET=LINUX64
export ARMCI_NETWORK=OPENIB
export MSG_COMMS=MPI

export USE_MPI=y
export USE_MPIF=y
export USE_MPIF4=y
export LARGE_FILES=TRUE
export USE_NOFSCHECK=TRUE

export  NWCHEM_LONG_PATHS=Y      #-- allow longer than 64 characters limit to N

export MKLROOT=/software/tools/compilers/intel/compilers_and_libraries_2016.1.150/linux/mkl

export HAS_BLAS=yes
export BLASOPT="${MKLROOT}/lib/intel64/libmkl_blas95_lp64.a -Wl,--no-as-needed -L${MKLROOT}/lib/intel64 -lmkl_gf_lp64 -lmkl_core -lmkl_sequential -lpthread -lm -ldl"

export USE_64TO32=y

export CC=pecc
export CXX=pec++
export F77=pefortran
export F90=pefortran
export F9C=pefortran
export F9X=pefortran
export F03=pefortran
export FC=pefortran

export PE_POST_FLAGS="-libverbs"
